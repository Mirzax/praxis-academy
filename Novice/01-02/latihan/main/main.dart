main(){
  var user = 'Zakku';
  var price = '12000';
  final proc = 'bill';
  final String procces = 'billing';

  const tet = 125;
  const double teta = 8.0 * tet;

  List<int> cost = const [21, 32, 45, 11];
  final pl = const ['peek', 'keep'];


  int pre = int.parse(price);
  var pro = double.parse('3.1415');
  var pra = pre.toString();
  String pri = pro.toStringAsFixed(1);


  var got = (cost[1] << 4 - 3) - (cost[2] >> 1) ;
  print(got);

}